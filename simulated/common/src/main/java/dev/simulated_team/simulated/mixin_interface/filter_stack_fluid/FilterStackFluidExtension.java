package dev.simulated_team.simulated.mixin_interface.filter_stack_fluid;

import net.minecraft.world.level.Level;

public interface FilterStackFluidExtension{
    boolean simulated$hasFluid(Level world);
}
